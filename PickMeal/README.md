# ⚠ 이 폴더는 배포에 쓰이지 않습니다

Vercel 은 저장소 **루트의 `index.html`** 을 서빙합니다.
이 폴더는 초기 구조 변경 때 남은 사본이며, 루트와 동일하게 유지만 하고 있습니다.

- 코드를 고칠 때는 **루트 `index.html`** 을 고치세요
- 메뉴 사진은 루트 `images/` 에만 있습니다 (여기서 열면 이모지로 표시됩니다)
- 마감 후 이 폴더는 삭제하는 것을 권합니다
